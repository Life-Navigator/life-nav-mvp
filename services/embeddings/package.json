{
  "name": "@life-navigator/embeddings",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "poetry run python -m app.main",
    "build": "poetry build",
    "test": "poetry run pytest -v --cov=app --cov-report=html",
    "lint": "poetry run ruff check . && poetry run black --check .",
    "clean": "rm -rf dist build *.egg-info"
  }
}
