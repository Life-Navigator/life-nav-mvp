{
  "name": "@life-navigator/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "poetry run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "build": "echo 'No build step needed for Python service'",
    "test": "poetry run pytest -v --cov=app --cov-report=html",
    "lint": "poetry run ruff check . && poetry run black --check .",
    "format": "poetry run black . && poetry run ruff check --fix .",
    "type-check": "poetry run mypy app/",
    "clean": "rm -rf dist build *.egg-info .pytest_cache"
  }
}
