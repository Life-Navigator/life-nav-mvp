# Top 20 Fixes (prioritized)

From the observability audit + live evaluation + GraphRAG/recommendation audits. Each: priority, root
cause, code location, recommended fix. **Discovery only — nothing changed yet.**

## P0 — before the 20 beta users

| #   | Fix                                                     | Root cause                                                                                                                                                     | Location                                                                                                                           | Recommended fix                                                                                                                                                                                          |
| --- | ------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **Persist `advisor.turns`**                             | No advisor conversation/turn log exists; all per-turn trust signals are discarded after the response.                                                          | `app/services/advisor_orchestrator.py:converse` (computes llm_status, prompt_version, validator reasons, relationships_referenced) | Write one row per turn (session_id, user_id, turn_index, message, response, prompt_version, llm_status, validator{ok,reasons}, relationships_referenced, latency_ms, tokens). Best-effort, non-blocking. |
| 2   | **Stop discarding the LLM on `more than one question`** | 17% of turns (all the high-value decision questions) fall back because the LLM asks 2 questions; validator rejects → generic rule reply.                       | `app/services/advisor_validator.py` (`next_q.count('?')>1` → reject); `advisor_llm.ADVISOR_SYSTEM`                                 | REPAIR instead of reject: trim to the first question and accept; and tighten the prompt ("exactly ONE question, no compound questions").                                                                 |
| 3   | **Fallback copy must address the question**             | On fallback the user gets a generic "Thanks — got it. In your own words, what are you working toward?" that ignores their decision question.                   | `app/services/relationship_manager.py` fallback text / `advisor_orchestrator._compose`                                             | When falling back on a decision question, name the missing inputs for THAT question instead of a generic discovery prompt.                                                                               |
| 4   | **Advisor latency ~9s (p95 11.5s)**                     | Gemini-on-Fly generation + `my_life` aggregation in the path; no streaming.                                                                                    | `my_life.my_life`, advisor turn path                                                                                               | Stream the response (buffer for governance, type it out — already a stated UX preference) and/or parallelize the `my_life` sub-calls + cache the snapshot per turn.                                      |
| 5   | **Apply the archetype cleanup migration to prod**       | `20260613010000_cleanup_archetype_risks.sql` deletes legacy archetype risk/opp rows but is not yet applied (display gates protect new+legacy in the meantime). | `supabase/migrations/20260613010000_*.sql`                                                                                         | `supabase db push` (or run in SQL editor) + `notify pgrst`.                                                                                                                                              |

## P1 — early beta

| #   | Fix                                            | Root cause                                                                                    | Location                                                        | Recommended fix                                                                                         |
| --- | ---------------------------------------------- | --------------------------------------------------------------------------------------------- | --------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| 6   | Wire CostMeter + latency into the advisor turn | Advisor LLM calls are unmetered (CostMeter not called from the advisor path).                 | `app/services/advisor_llm.py:generate`, `app/clients/gemini.py` | `perf_counter` around generate; record `usageMetadata` tokens via CostMeter into `ops.llm_usage_meter`. |
| 7   | Data-rich eval personas                        | Fresh-user sample can't measure recommendation coverage/quality.                              | `apps/web/advisor-eval.mjs`                                     | Seed finance accounts + a 401k/insurance doc for a few personas; measure rec count + evidence quality.  |
| 8   | Seeded-graph persona in the standing eval      | The _grounded_ GraphRAG citation path isn't exercised by fresh users (only the abstain path). | `advisor-eval.mjs`                                              | Add a persona with a seeded connected graph; assert the advisor cites a real edge.                      |
| 9   | Cross-session memory test                      | In-session retention works; long-term recall is unverified (no turn log).                     | depends on #1                                                   | After #1, replay a returning user and verify prior facts are recalled + not re-asked.                   |
| 10  | Explicit `missing_inputs` per recommendation   | "What data is missing" is implicit (`unlocked_capabilities` + conversational).                | `app/services/recommendations_os.py`                            | Add a `missing_inputs` field to the rec row + surface it.                                               |
| 11  | Per-turn GraphRAG retrieval log                | We enforce citation validity but don't store what was retrieved per turn.                     | `advisor_context` / `advisor_orchestrator`                      | Persist `{node_ids, edge_ids, document_ids, recommendation_ids}` on the turn (part of #1).              |
| 12  | Request correlation ids                        | No trace id across proxy → Core → model; logs aren't correlated.                              | `apps/web` middleware/proxy + `app/main.py`                     | Generate a request id at the proxy, forward as a header, include in Core logs + the turn row.           |

## P2 — hardening / completeness

| #   | Fix                                                       | Root cause                                                                                                                                 | Location                                                      | Recommended fix                                                                                 |
| --- | --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| 13  | First-class provenance columns                            | provenance_type is derived in the API, not stored; `public.goals`, `life.risks/opps/deps/constraints`, `finance.*` lack source/confidence. | new migration                                                 | Add `provenance_type`/`source`/`confidence`/`updated_at` columns + backfill; writers set them.  |
| 14  | `<ProvenanceBadge>` on every dashboard item               | Currently only on the north-star objective.                                                                                                | `ExecutiveSummary`, `DashboardClient` cards, goals list, recs | Reuse `apps/web/src/components/ui/ProvenanceBadge.tsx` everywhere a fact is rendered.           |
| 15  | Validator-outcome + fallback-rate metrics                 | No counters for how often the validator rejects / why.                                                                                     | `advisor_orchestrator` → `analytics.events`                   | Emit a metric per turn (enhanced vs fallback:reason).                                           |
| 16  | Read-only `/v1/ops/advisor` view                          | QA/compliance can't inspect a user's sessions.                                                                                             | new router                                                    | After #1, expose session→turns + provenance for support/compliance.                             |
| 17  | Finance source/confidence                                 | `finance.financial_accounts`/`net_worth_snapshots` lack source/confidence (Plaid vs manual implicit).                                      | `031_finance_domain` / writers                                | Add `source`/`confidence`; label Plaid vs persona vs manual.                                    |
| 18  | Expose document provenance in API                         | `document_fields` link to the parent doc only by FK.                                                                                       | documents API                                                 | Return `source_document` (name + doc_type) with each field.                                     |
| 19  | LifeIntelligence → consume my-life                        | "Your Life Model" widget reads raw `/api/life/snapshot` + a frontend archetype denylist (drift risk).                                      | `apps/web/src/components/dashboard/LifeIntelligence.tsx`      | Point it at the grounded `/api/life/my-life`; delete the frontend `ARCHETYPE_RISK_OPP` mirror.  |
| 20  | Promote `/life-graph/explainable` → default `/life-graph` | New explainable graph baking alongside the old one.                                                                                        | `apps/web/src/app/life-graph`                                 | After parity bake, swap the route + wire the placeholder sidebar items / decision-tree overlay. |

## Security (do now)

- **Rotate the Supabase personal access token** (`sbp_…`) pasted in chat — Supabase → Account → Access Tokens → revoke.

## Note on success criteria

All deterministic Success Criteria **passed** in the live run (no invented goals/risks/opps/recs, no
provenance violations, no rejected-goal resurfacing, no ungrounded retirement risks, no archetype leakage).
The fixes above are about **observability + experience + completeness**, not trust-integrity regressions.
