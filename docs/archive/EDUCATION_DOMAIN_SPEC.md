# EDUCATION DOMAIN SPEC (Elite buildout, Phase 5 — LAST)

**Date:** 2026-06-07 · **Status:** DESIGN ONLY. Build order: Finance→Health→Career→Family→**Education** (last).

Education ROI + learning-path intelligence: degrees, certifications, courses, program ROI.

---

## 1. Supabase tables

**Existing:** `public.education_records`, `public.courses`, `public.degree_analyses`.
**Missing (➕):** `public.education_profile`, `public.education_goals`, `public.certifications` (verify vs derived), `public.degrees`, `public.programs`, `public.tuition_costs`, `public.education_roi_models`, `public.education_recommendations`.
Pattern: migration-116; `user_id`, `updated_at`, indexes.

## 2. Worker entity types

**Present:** `education_record, course, education_intake, study_log`.
**Add (➕):** `education_profile, education_goal, certification, degree, program, tuition_cost, education_roi_model, education_recommendation`. Enum-before-trigger.
Labels: :EducationProfile, :EducationGoal, :Course, :Certification, :Degree, :Program, :TuitionCost, :EducationROIModel, :EducationRecommendation.
Relationships: (:UserProfile)-[:HAS_EDUCATION]->(:EducationProfile)-[:PURSUING]->(:Program); (:Program)-[:GRANTS]->(:Degree|:Certification); (:Program)-[:HAS_COST]->(:TuitionCost); (:EducationROIModel)-[:EVALUATES]->(:Program); (:Certification)-[:CLOSES]->(:Skill) (cross-domain Career); (:EducationGoal)-[:FUNDED_BY]->(:FinancialGoal) (cross-domain Finance). Freshness: on-change.

## 3. Backend endpoints (`domains/education.py`)

```
GET  /v1/education/summary  /profile  /goals  /programs  /roi  /recommendations
POST /v1/education/goal  /program  /course
```

`roi` returns ROI models per program (cost vs projected comp uplift, payback period) — cross-links Career compensation + Finance.

## 4. UI surfaces

Education Overview (hero: active learning, credential progress, top ROI opportunity, next milestone, next move) · Learning Path · Degree ROI · Certification ROI · Courses · Programs · Education Recommendations.

## 5. Recommendations (H contract)

highest-ROI-credential-for-career-goal · certification-vs-degree-tradeoff · course-completion-nudge · tuition-funding-plan (cross-domain Finance/Family). Generated by **Education Agent**, ranked by Recommendation Agent.

## 6. Chat

"Is this degree worth it?" (ROI model) · "Which certification has the best ROI for my career goal?" · "What's my learning path?" · "How do I fund this program?" — grounded in education_profile + programs + roi_models + cross-domain Career/Finance.

**Unlock:** sidebar "Education" → live when summary renders real data. Education is intentionally last because its ROI models depend on mature Career (compensation) + Finance (funding) data.
