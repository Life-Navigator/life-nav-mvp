# ARCANA_CITATION_CHIPS.md — Phase 2

## Delivered

Source chips render below each answer, mapped from `citation.sourceTable`/`domain` to friendly labels:
`life.facts`/`documents.documents` → **Document**, `career.career_goals` → **Career Goal**,
`finance.financial_accounts` → **Finances**, `family.dependents` → **Family**, education → **Education Goal**, etc.
Distinct (deduped), compact, clickable → opens the evidence drawer (`Why?`).

## Verified live

Promotion answer showed chips: **Career Goal · Education Goal · Finances · Family · Document**. Clicking opens the Sources section in the drawer (label + value + confidence). No raw `sourceTable` strings, no markdown.
