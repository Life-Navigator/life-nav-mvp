# ARCANA_GOAL_CHIPS.md — Phase 5

## Delivered

Relevant goals render as compact emerald chips, from `candidate_goals` (plumbed as `goals`).

## Note (honest)

On the promotion turn the surfaced chips were dominated by risks + source/goal labels (Career Goal, Education Goal) — the `candidate_goals` list is populated when the turn proposes goals; otherwise the goal _context_ shows via the Career Goal / Education Goal source chips. The emerald goal-chip path is wired and renders whenever `candidate_goals` is non-empty.
