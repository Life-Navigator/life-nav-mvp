apiVersion: v1
kind: Service
metadata:
  name: agents
  labels:
    app: agents
    component: orchestration
spec:
  type: ClusterIP
  selector:
    app: agents
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  sessionAffinity: None
