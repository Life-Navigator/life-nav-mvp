apiVersion: v1
kind: Service
metadata:
  name: mcp-server
  labels:
    app: mcp-server
    component: protocol
spec:
  type: ClusterIP
  selector:
    app: mcp-server
  ports:
  - name: http
    port: 8090
    targetPort: 8090
    protocol: TCP
  sessionAffinity: None
