{
  "name": "supabase-proxy-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for Supabase storage proxy",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "build": "esbuild src/index.ts --bundle --outfile=dist/index.js --format=esm",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "esbuild": "^0.19.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.22.0"
  }
}
